# CI, workflows and issue templates are maintainer-owned.
#
# Contributors should not modify anything under .github/. Issues are scoped so
# that the deliverable is a script, test or config the pipeline can call — a
# maintainer wires it into the pipeline separately.
#
# Enforcement requires branch protection on `main` with "Require review from
# Code Owners" enabled; without that, this file only requests review
# automatically rather than blocking the merge.

/.github/ @pope-h
